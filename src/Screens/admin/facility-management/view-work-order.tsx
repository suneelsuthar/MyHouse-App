import React from "react";
import { Screen, Text } from "../../../Components";

export const AdminFMViewWorkOrder = () => {
  return (
    <Screen preset="fixed" safeAreaEdges={["top"]}>
      <Text text="AdminFMViewWorkOrder" />
    </Screen>
  );
};
